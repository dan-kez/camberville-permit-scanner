[project]
name = "permit-scanner"
version = "0.1.0"
description = "Scan building permits to find homes likely coming to market"
requires-python = ">=3.12"
dependencies = [
    "argcomplete>=3.6.3",
    "requests>=2.32.5",
    "tabulate>=0.9.0",
]
